# Language Module

This folder owns the language feature as a single unit:

- `languageData.ts`: English source data
- `languageFunctions.ts`: lookups, search helpers, filtering, country mappings
- `translations/`: localized language labels used by the shared translation layer
- `language.test.ts`: module tests
- `index.ts`: public exports for the package root
